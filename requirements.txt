# Core dependencies
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
sqlalchemy>=2.0.0
python-jose[cryptography]>=3.3.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-multipart>=0.0.9  # required by FastAPI for form/file handling

# TrueData market data + Excel export
truedata-ws>=5.0.0       # official TrueData WebSocket + REST historical SDK
pandas>=2.0.0            # DataFrame intermediate representation
xlwt>=1.3.0              # writes legacy .xls (BIFF8) files
openpyxl>=3.1.0          # fallback reader for any xlsx assets; also pulls in xlrd-free path

# Dev / test dependencies
pytest>=8.0.0
httpx>=0.27.0
